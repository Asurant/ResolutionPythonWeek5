[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "resolution_week5_Asurant"
version = "0.0.1"
dependencies = [ "fastapi[standard]", "slowapi" ]

[project.scripts]
my-app = "resolution_week5_Asurant.main:main"